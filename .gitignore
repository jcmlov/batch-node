# Dependency directories (용량이 크고 npm install로 생성 가능하므로 제외)
node_modules/
jspm_packages/

# Debug log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS metadata
.DS_Store
Thumbs.db

# 환경 변수 파일 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs (빌드 결과물)
dist/
build/
out/

# IDE settings (개인 설정 파일)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?